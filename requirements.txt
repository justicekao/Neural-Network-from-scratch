# NEEDS YOUR INPUT: nothing, mirrors pyproject.toml. Prefer
# `pip install -e .` (see README) since it also registers the
# `genmonod-app` command.

numpy>=1.23
scipy>=1.9
pandas>=1.5
matplotlib>=3.6
streamlit>=1.30
torch>=2.0
pytest>=7.0
