# NEEDS YOUR INPUT: nothing, this mirrors pyproject.toml dependencies.
# Use this if you just want `pip install -r requirements.txt` without
# installing the package itself. `pip install -e .` (see README) is the
# preferred route since it also gives you the `monod_pinn` import.

torch>=2.0
numpy>=1.23
scipy>=1.9
pandas>=1.5
matplotlib>=3.6
tqdm>=4.64
pytest>=7.0
